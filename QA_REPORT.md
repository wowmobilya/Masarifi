# Masarifi V5 — تقرير التحقق

التاريخ: 25 سبتمبر 2026

**النتيجة: 42 اختبارًا آليًا نجحت، 0 فشل.**

## إصلاح 5.0.1

- فُحص المستودع العام `wowmobilya/Masarifi`: الصفحة الرئيسية موجودة، لكن مجلد `assets` غير مرفوع، وطلب `assets/v5-app.js` يعيد 404. هذا يطابق الشاشة بلا تنسيق.
- تغيّر البناء ليضم الأكواد والتنسيق وصور الواجهة داخل `index.html`. الأيقونات المطلوبة للتثبيت موجودة في جذر الحزمة.
- أضيف اختبار تحميل HTML الناتج كاملًا مع فرض 404 على الموارد الخارجية: يبدأ بصندوق واحد دون بيانات سابقة وتظهر الواجهة بتنسيقها.
- أضيف اختبار زر التثبيت في الرئيسية واستدعاء طلب المتصفح وإخفائه بعد حدث التثبيت. هذه محاكاة لواجهة المتصفح وليست تثبيتًا فعليًا على الهاتف.
- تسجيل الخدمة يبلغ عن فشل تجهيز العمل دون اتصال، بدل إبقاء حالة التجهيز معلقة. تعليمات iPhone وAndroid متاحة عند غياب الطلب المباشر.

## ما جرى اختباره

- محرك مساعد محلي بخمس لغات: الفترات، العملات، التصنيفات، السؤال عن أكبر مصروف، الميزانية، الأرصدة الفعلية، متابعة السياق، والتمييز بين التاريخ والمبلغ.
- عدم إضافة عمليات مالية من المحادثة دون مراجعة نموذج الحفظ.
- بدء التثبيت الجديد بصندوق TRY واحد، ورصيد صفر، مع الاحتفاظ بحسابات النسخة السابقة.
- إنشاء الصفحات السبع بكل لغة (35 تركيبًا) دون أخطاء تشغيل في DOM المحاكى، بما في ذلك الفرنسية والكردية.
- نموذج إضافة المصروف وحفظ 125.50 بشكل صحيح، وتذكر الصندوق، وإعادة فتح قاعدة البيانات دون فقد السجل.
- التحويلات المزدوجة والرسوم والحسابات بوحدات صحيحة، دون جمع عملات مختلفة.
- نموذج تحويل بسعر مرجعي يحتاج مرحلتي المراجعة والتأكيد، ويعرض مصدر TCMB الصحيح.
- بطاقات الفترات الأربع مستقلة عن فلاتر السجل السابقة، وحدود التواريخ صحيحة.
- استيراد نسخة V4 بتصنيفات ذات ثلاث لغات فقط، واستعادة الإعدادات واللغة والفترات.
- جلب الأسعار الناجح والفاشل بمدخلات اختبار: لا يُستبدل آخر تحديث صحيح عند تعذّر الاتصال، وتُرفض القيم السالبة والمستقبلية وغير المتجانسة.
- تحقق حي منفصل من استجابة ECB وTCMB عبر Frankfurter، ومن ترويسة `Access-Control-Allow-Origin: *`.
- ضغط صورة اختبار 4000×3000 إلى JPEG 2000×1500 تحت حد الحجم، مع المحافظة على النسبة.
- إنشاء PDF صالح البنية وملف XLSX حقيقي قابل للتحرير؛ فُك ضغط المصنف للتحقق من النص الفرنسي والقيمة الرقمية 123.45.
- التحقق من كلمة المرور، ورفض الكلمة الخاطئة، والقفل بعد إعادة الفتح.
- إغلاق حالات تسرب محتوى النافذة بعد القفل، واستمرار فتح الجلسة القديم بعد إعادة قفلها، واستثناء اختيار الصور بعد إلغائه.
- خيارات اللغات الخمس في مشاركة العملية أو كشف الحساب الفردي.
- ملفات كاش التشغيل دون اتصال موجودة، والتنقل داخل مسار مستودع GitHub يعيد الصفحة المحلية في المحاكاة؛ لا يجري تخزين طلبات الأسعار أو نسخ المستخدم وملفات تقاريره في كاش التطبيق.
- تحليل صياغة JavaScript في نسخة HTML المجمّعة: 13 كتلة برمجية سليمة.

## منهج الاختبار وحدوده

الاختبارات تستخدم Node.js، وJSDOM لمحاكاة DOM، وfake-indexeddb، وcanvas محلي لتصدير التقارير. جرى أيضًا فحص مستقّل للكود وإصلاح الملاحظات المهمة قبل الاختبار النهائي.

**لم تُنجز معاينة بصرية حقيقية داخل متصفح، ولا تجربة تثبيت PWA أو كاميرا أو مشاركة نظام على جهاز فعلي**: بيئة المعاينة منعت فتح الملفات/العنوان المحلي. لذلك لا يعد هذا التقرير ضمانًا بأن جميع الأجهزة أو المتصفحات خالية من الأخطاء، ولا توجد نتيجة قياس سرعة فعلية على تابلت المستخدم. CSS يتضمن أحجام الهاتف والتابلت والكمبيوتر، ودعم RTL والليل والنهار، لكنه يحتاج مراجعة بصرية على الجهاز بعد الرفع.

الفرنسية والكردية الكرمانجية لم تخضعا لمراجعة لغوية بشرية متخصصة. القفل محلي ولا يشفر قاعدة البيانات. المساعد محلي محدد الوظائف وليس نموذج محادثة سحابيًا عامًا. أسعار الصرف مرجعية يومية، والمعادن تحتاج إدخالًا يدويًا.

## إعادة الاختبار

```bash
npm install
npm test
```

تحديث ملفات النشر بعد تعديل المصدر:

```bash
python3 tools/build.py
```

لا تحتاج نسخة التشغيل المنشورة إلى هذه الحزم أو الأوامر.
