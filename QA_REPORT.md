# مصاريفي 7.0.0 — تقرير التحقق

التاريخ: 26 سبتمبر 2026. **148 اختبارًا ناجحًا، 0 فشل، 0 تخطي** في البناء النهائي `f0e9909b1371`. السجل الكامل في `docs/test-results.txt`.

## الاختبارات

- 104 اختبارات موروثة للتشغيل المجمّع دون مجلد assets، الأرصدة والتحويلات والرسوم، الفترات الأربع، الاستيراد، اللغات، الصور والتقارير، القفل والجلسات، المساعد، Drive، التثبيت والتحديثات.
- 11 اختبارًا للصناديق: صفحات مستقلة، أولوية الحساب الأساسي/الاختيار الصريح، استبعاد المعادن من الأساسي، الحساب المحذوف، العملات والوحدات الصحيحة، حفظ الاختيار وإعادة الفتح، التصفية والتصدير، إعادة ربط الحساب الأساسي عند استيراد نسخة، وحد الثمانية وحذف غير المستخدم وربط البيانات القديمة.
- 13 اختبارًا جديدًا للمساعد: الأوامر بخمس لغات، المقارنات، الملاحظة وقائمة المهام، البحث، اختصارات عربية عامية محددة، التنقل، تصحيح مسودة دون تغيير السجلات، أولوية الحساب والعملة، وعدم التنفيذ من نتائج متأخرة بعد مغادرة الصفحة أو القفل.
- 15 اختبارًا للملاحظات: البيانات القديمة دون تعديل عند القراءة، حفظ/تصدير/استعادة الحقول الجديدة، التحقق من النسخ، النص غير النشط، البحث والتثبيت والأرشفة، الصور والمشاركة والتقرير والسلة، تعارض التعديلات، إلغاء حفظ جلسة سابقة، واستمرار مسودات الجلسة عبر الرجوع.
- 5 اختبارات للرئيسية والتنقل: إخفاء جميع العناصر مع إمكانية إعادتها، حفظ الاختيارات وإعادة التحميل، دائرة تنقل واحدة مستمرة بالاتجاهين، استعادة حساب الصفحة السابقة، وترجمة صفحة التخصيص باللغات الخمس.

## المراجعة والإصلاحات

مراجعة مستقلة وجدت أن الصور المضافة عبر مسار اختيار الملف الحقيقي قد تضيع عند إعادة رسم محرر الملاحظة. أعاد اختبار المشكلة إنتاجها، وأُصلح التقاط الصورة بعد انتهاء عملية الإضافة غير المتزامنة. أصبح الاختبار يضيف الصورة عبر `PhotoEditor.add()` ويعيد رسم الصفحة ثم يتحقق من بقاء الصورة وإمكان حفظها.

أُصلح أيضًا الرجوع بعد حفظ ملاحظة جديدة حتى يعود إلى الملاحظة المحفوظة، وإكمال حفظ متأخر دون سحب المستخدم من الصفحة التي انتقل إليها، وإعادة رسم صور من جلسة قديمة بعد القفل. أضيفت اختبارات لحالاتها.

فُحصت صياغة 26 كتلة JavaScript و8 أوراق CSS في HTML الناتج. تطابق إصدار package.json وrelease.json، وتوجد أيقونات البيان الثلاث. ملفات ألوان V6 الأصلية `base.css/v5.css/v6.css` مطابقة بايتًا للنسخة السابقة؛ درجات V7 الجديدة مقيدة بالوضع النهاري. التغييرات في التخطيط والتنقل تعمل في الوضعين بألوان كل وضع.

## حدود التحقق

هذا تحقق آلي باستخدام Node.js وJSDOM وfake-indexeddb وcanvas، ومراجعة كود مستقلة. **لم تُنجز معاينة بصرية حقيقية للواجهة داخل متصفح أو على هاتف/تابلت فعلي** بسبب قيود بيئة المعاينة المحلية. لا توجد نتيجة قياس سرعة فعلية على جهاز المستخدم. التثبيت والإملاء الصوتي والربط الحقيقي مع حساب Google لم تُجرّب على جهاز أو حساب المستخدم. تفحّص شكل الواجهة والتثبيت والصوت بعد رفع الحزمة.

المساعد نصي محلي بالقواعد، وليس نموذج محادثة عامًا. تعرف الصوت يعتمد على المتصفح/اللغة وقد يحتاج إنترنت. الفرنسية والكرمانجية لم يدققهما مختص لغوي بشري. القفل محلي لا يشفر قاعدة البيانات. مسودات الملاحظات غير المحفوظة تبقى داخل جلسة الصفحة وتُمسح عند القفل أو إعادة التحميل، مع طلب تحذير المغادرة عند وجود تغييرات.

بقيت آلية Drive وحدودها: إعداد حسابك مرة واحدة، حد جسر 15 ميبيبايت، وملف نسخة واحد. جدول Google يجدد آخر نسخة مرفوعة ولا يسحب تغييرات جهاز مغلق. لا تتضمن هذه الحزمة نشرًا إلى GitHub أو تفعيل Google نيابة عن المستخدم.

## إعادة الاختبار

```bash
npm install
npm run build
npm test
```

ارفع ملفات النشر السبعة معًا. الملفات المطلوبة والتعليمات في README_AR.md. لا يلزم مسح بيانات المتصفح للتحديث.
